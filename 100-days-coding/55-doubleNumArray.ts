
// starts with the list of numbers
let numbers: number[] = [1, 2, 3, 4, 5];

// doubles each number
let doubledNumbers = numbers.map(number => number * 2);

// shows the new list of doubled numbers
console.log(doubledNumbers); 
